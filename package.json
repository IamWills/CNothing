{
  "name": "cnothing",
  "version": "0.1.5",
  "description": "CNothing AuthAI + Encrypted KV server and SDK for AI-safe secret workflows",
  "type": "module",
  "main": "./dist/library.js",
  "types": "./dist/library.d.ts",
  "exports": {
    ".": {
      "types": "./dist/library.d.ts",
      "import": "./dist/library.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "README.CN.MD"
  ],
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun run src/index.ts",
    "migrate": "bun run src/db/migrate.ts",
    "generate-secrets": "bun run src/scripts/generate-secrets.ts",
    "build": "tsc -p tsconfig.build.json",
    "prepare": "bun run build",
    "console:dev": "cd console && bun run dev",
    "console:build": "cd console && bun run build",
    "console:start": "cd console && bun run start",
    "github:bootstrap": "bun run examples/github-connector/bootstrap.ts",
    "github:connector": "bun run examples/github-connector/index.ts",
    "github:agent:register": "bun run examples/github-agent/register.ts",
    "slack:bootstrap": "bun run examples/slack-connector/bootstrap.ts",
    "slack:connector": "bun run examples/slack-connector/index.ts",
    "webhook:bootstrap": "bun run examples/webhook-connector/bootstrap.ts",
    "webhook:connector": "bun run examples/webhook-connector/index.ts",
    "search:bootstrap": "bun run examples/search-connector/bootstrap.ts",
    "search:link": "bun run examples/search-connector/link.ts",
    "e2e:v2": "bun run examples/e2e-v2/run.ts",
    "typecheck": "bunx tsc --noEmit",
    "lint": "bunx eslint src --ext .ts"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/pg": "^8.15.6",
    "@typescript-eslint/eslint-plugin": "^8.16.0",
    "@typescript-eslint/parser": "^8.16.0",
    "bun-types": "^1.3.7",
    "eslint": "^9.16.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "bun": ">=1.3.0"
  }
}
