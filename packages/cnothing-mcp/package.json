{
  "name": "cnothing-mcp",
  "version": "4.0.0",
  "description": "CNothing V4 stdio MCP client for user-approved, credential-injecting OAuth API access without exposing tokens to agents.",
  "type": "module",
  "bin": {
    "cnothing-mcp": "./src/index.ts"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "start": "bun run src/index.ts"
  }
}
