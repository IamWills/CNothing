# cnothing-mcp

The stdio adapter now lives in `agenttools/mcp`. This directory keeps a compatibility entry:

```bash
bun run packages/cnothing-mcp/src/index.ts
```

See [agenttools/mcp/README.md](../../agenttools/mcp/README.md).
